# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database

# Session Configuration
SESSION_SECRET=your-random-session-secret-here

# Environment
NODE_ENV=production

# Server Configuration (optional)
PORT=5000

# Instructions:
# 1. Copy this file to .env for local development
# 2. Replace the values with your actual database credentials
# 3. Generate a secure session secret (e.g., using: openssl rand -base64 32)
# 4. For production deployment, set these as environment variables in your hosting platform